export const dynamic = 'force-dynamic'
import { StepDetailPage } from '@/components/steps/StepDetailPage'

export const metadata = { title: 'Build / Validate — AI PDLC Workflow Tracker' }

export default function BuildValidatePage() {
  return <StepDetailPage slug="build-validate" />
}
