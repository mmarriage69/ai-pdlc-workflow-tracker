export const dynamic = 'force-dynamic'
import { StepDetailPage } from '@/components/steps/StepDetailPage'

export const metadata = { title: 'Measure / Iterate — AI PDLC Workflow Tracker' }

export default function MeasureIteratePage() {
  return <StepDetailPage slug="measure-iterate" />
}
